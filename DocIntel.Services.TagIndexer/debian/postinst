#!/bin/bash
#
set -e

. /usr/share/debconf/confmodule

chown -R docintel:docintel /usr/lib/docintel/tag-indexer/

cp /usr/share/doc/docintel/nlog.config.worker-example /usr/lib/docintel/tag-indexer/nlog.config
sed -i "s,WORKER_NAME,tag-indexer,g" /usr/lib/docintel/tag-indexer/nlog.config

exit 0


