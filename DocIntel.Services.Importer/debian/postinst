#!/bin/bash
#
set -e

. /usr/share/debconf/confmodule

chown -R docintel:docintel /usr/lib/docintel/importer/

cp /usr/share/doc/docintel/nlog.config.worker-example /usr/lib/docintel/importer/nlog.config
sed -i "s,WORKER_NAME,importer,g" /usr/lib/docintel/importer/nlog.config

exit 0


